"use client";

import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { api } from "@/lib/api";

export default function LoginPage(){const[username,setUsername]=useState("admin");const[password,setPassword]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");try{await api("/auth/login",{method:"POST",body:JSON.stringify({username,password})});router.push("/");router.refresh()}catch(err){setError(err instanceof Error?err.message:"登录失败")}finally{setLoading(false)}}return <div style={{minHeight:"100vh",display:"grid",placeItems:"center",padding:20}}><div className="card" style={{width:"min(430px,100%)",padding:28}}><div className="brand" style={{padding:"0 0 24px"}}><div className="brandStone">Z</div><div><strong>ZenoMinerals</strong><span>自媒体运营平台</span></div></div><div className="eyebrow">Secure workspace</div><h1 style={{fontSize:26,margin:"8px 0"}}>登录</h1><p style={{color:"var(--muted)",fontSize:12,lineHeight:1.6}}>此登录只属于当前独立网站。</p>{error?<div className="error">{error}</div>:null}<form className="form" style={{gridTemplateColumns:"1fr"}} onSubmit={submit}><div className="field"><label>用户名</label><input className="input" value={username} onChange={e=>setUsername(e.target.value)} /></div><div className="field"><label>密码</label><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoFocus /></div><button className="button" disabled={loading}>{loading?"登录中…":"进入工作台"}</button></form></div></div>}
